default: &default
  adapter: postgresql
  encoding: unicode
  host: <%= ENV.fetch('PG_HOST', '127.0.0.1') %>
  username: <%= ENV.fetch('PG_USERNAME', 'postgres') %>
  password: <%= ENV['PG_PASSWORD'] %>
  pool: <%= ENV.fetch('RAILS_MAX_THREADS', 5) %>
  port: <%= ENV.fetch('PG_PORT', 5432) %>

development:
  <<: *default
  database: document_editor_development

test:
  <<: *default
  database: document_editor_test
production:
  primary: &primary_production
    <<: *default
    database: document_editor_production
    username: document_editor
    password: <%= ENV["DOCUMENT_EDITOR_DATABASE_PASSWORD"] %>
  cache:
    <<: *primary_production
    database: document_editor_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: document_editor_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: document_editor_production_cable
    migrations_paths: db/cable_migrate
